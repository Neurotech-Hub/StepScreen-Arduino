// StepScreen facade is header-only (inline) so that pin macro defaults
// resolve in the sketch's translation unit, keeping sketch-level pin
// overrides consistent. See StepScreen.h.
#include "StepScreen.h"
